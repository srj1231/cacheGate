spring:
  application:
    name: cachegate

  main:
    banner-mode: off

  ai:
    mcp:
      server:
        stdio: true
        name: cachegate
        version: 0.1.0

    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-3.6-flash
